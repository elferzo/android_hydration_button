<?xml version="1.0" encoding="utf-8"?>
<!--
    ВАЖНО: плоский LinearLayout — из опыта Price Widget v2.
    Вложенные контейнеры → setTextViewText молча игнорируется RemoteViews.
    Корневой FrameLayout нужен только чтобы задать фон кнопки через отдельный View.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/btn_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1E88E5"
    android:padding="8dp">

    <!-- Вертикальный стек: надпись + счётчик -->
    <!-- Плоский LinearLayout внутри FrameLayout — RemoteViews находит оба TextView напрямую -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="@android:color/transparent">

        <TextView
            android:id="@+id/tv_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="H2O"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="#FFFFFF"
            android:textSize="28sp"
            android:textStyle="bold"
            android:layout_marginTop="2dp" />

    </LinearLayout>

</FrameLayout>
